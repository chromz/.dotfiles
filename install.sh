#/bin/bash
mkdir ~/.config
cp nvim ~/.config/ -r
cp .vim/after/ ~/.local/share/nvim/ -r
cp bspwm ~/.config -r
cp dunst ~/.config -r
cp termite ~/.config -r
cp rofi ~/.config -r
cp xinitrc ~/.xinitrc
cp .tmux.conf ~/
cp cava ~/.config -r
cp polybar ~/.config -r
cp sxhkd ~/.config -r
mkdir ~/.config/compton
cp compton.conf ~/.config/compton

