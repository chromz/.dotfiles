#/bin/bash

mkdir ~/.config
cp .vimrc ~/.vimrc
cp .vim ~/.vim -r
cp bspwm ~/.config -r
cp dunst ~/.config -r
cp termite ~/.config -r
cp rofi ~/.config -r
cp xinitrc ~/.xinitrc
cp .vimrc ~/ -r
cp .tmux.conf ~/
cp cava ~/.config -r
cp awesome ~/.config -r
cp picom ~/.config -r
