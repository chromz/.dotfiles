#/bin/bash
mkdir ~/.config
cp bspwm ~/.config -r
cp dunst ~/.config -r
cp termite ~/.config -r
cp rofi ~/.config -r
cp xinitrc ~/.xinitrc
cp .tmux.conf ~/
cp nvim ~/.config -r
cp cava ~/.config -r
cp polybar ~/.config -r
cp sxhkd ~/.config -r
mkdir ~/.config/compton
cp compton.conf ~/.config/compton

