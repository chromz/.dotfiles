#/bin/bash
mkdir ~/.config
# cp .vimrc ~/.vimrc
# cp .vim ~/.vim -r
cp bspwm ~/.config -r
cp dunst ~/.config -r
cp termite ~/.config -r
cp rofi ~/.config -r
cp xinitrc ~/.xinitrc
cp nvim ~/.config/nvim -r
cp .tmux.conf ~/
cp cava ~/.config -r
cp polybar ~/.config -r
cp sxhkd ~/.config -r
mkdir ~/.config/compton
cp compton.conf ~/.config/compton

